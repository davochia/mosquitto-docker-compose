apiVersion: v1
kind: Route
metadata:
  name: mosquitto-route
spec:
  tls:
    termination: passthrough
  host: mqtt.student480.rahtiapp.fi
  port:
    targetPort: 8883
  to:
    kind: Service
    name: mosquitto-service
